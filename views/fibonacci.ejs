<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fibonacci Sequence Table</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: center;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body style="background-color: gray;">
    <h1>Fibonacci Sequence Table</h1>
    <table>
        <thead>
            <tr>
                <th colspan="<%= table[0].length %>">Fibonacci Sequence</th>
            </tr>
        </thead>
        <tbody>
            <% table.forEach(row => { %>
                <tr>
                    <% row.forEach(value => { %>
                        <td><%= value %></td>
                    <% }) %>
                </tr>
            <% }) %>
        </tbody>
    </table>
    <a href="/index">Back</a>
</body>
</html>
